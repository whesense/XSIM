# Config mixin: SkyLogit. Appends the model mixin (via list concatenation)
# and contributes its optimizer param. Import after the node base.
model_type:
  mixins:
    - _type_: xsim.modeling.gaussian.SkyLogit
opt_cfg:
  params:
    sky_logit:
      lr: 0.001
