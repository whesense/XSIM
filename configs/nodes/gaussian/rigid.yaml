# Rigid objects gaussian node (node-kind aspect only).
# Compose with a feature (features/*.yaml) and a strategy (strategy/*.yaml).
_fn_: xsim.modeling.nodes.gaussian.rigid_node.RigidSceneNode.create
return_velocity: true
model_type:
  _fn_: xsim.modeling.gaussian.compose
  mixins:
    - _type_: xsim.modeling.gaussian.Instanced
opt_cfg:
  _import_: ../common/opt/gaussian.yaml
